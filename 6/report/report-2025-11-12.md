# Отчет к 2025-11-12

- Обучил схожую сверточно-рекурсивную нейронную сеть на датасете с предложениями "iam-sentences"
- По результатам обучения можно сделать вывод, что сверточно-рекурсивная архитектура, протестированная на отдельных словах, неплохо себя показывает и на предложениях
- При обучении произошел явный выход на плато со значением CTC Loss на тренировочной выборке примерно в 0.3, в то время как на словах удавалось добиться 0.05 - вероятно, для лучшего результата требуется усложнить модель, но это повлечет увеличение времени на обучение, поэтому пока оставляю как есть
- В файле `src/iam-sentences-crnn/example/validation.png` можно посмотреть примеры того, что напредсказывала модель на элементах валидационной выборки: в подписях к графикам форма "[точная метка]: предсказание модели"
- Далее планируется разработать подход к разделению документа на строки - тогда каждая строка подается в модель `IamSentencesCRNN`, а выходы собираются в расшифровку всего документа
- На первом этапе попробую сделать это с помощью `OpenCV` - гипотеза состоит в том, что скорее всего на хорошей черно-белой бинаризированной фотографии документа распределение построчных сумм пикселей позволить определить границы отдельных строк - между строк будет пустое место или лишь "петельки" от некоторых букв.
